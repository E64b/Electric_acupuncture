#include "main.h"


uint16_t getExit(const uint16_t *pChar, uint16_t index) { 
return pChar[index];
}

void work() {
  if (data.work) {
    const uint8_t *GetProg(data.program);
    uint16_t exit = getExit(img, i);

    data.send = true;
  }
}
